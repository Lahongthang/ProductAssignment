import { Box, styled } from "@mui/material";

export const RootStyle = styled(Box)(() => ({
    gap: '16px',
    width: '100%',
    height: '100%',
    display: 'flex',
    paddingTop: '16px',
    flexDirection: 'column',
}));
